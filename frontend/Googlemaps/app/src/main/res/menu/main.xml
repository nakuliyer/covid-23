<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MapsActivity">

    <!-- Stats Icon -->
    <item
      android:id="@+id/action_statistics"
      android:icon="@drawable/ic_baseline_bar_chart_24"
      android:onClick="onGoToStatistics"
      android:title=""
      app:showAsAction="always" />

    <!-- Contact Tracing Icon -->
    <item
        android:id="@+id/action_contact_tracing"
        android:icon="@drawable/ic_baseline_people_alt_24"
        android:onClick="onGoToContactTracing"
        android:title=""
        app:showAsAction="always" />

    <!-- Settings Icon -->
    <item
        android:id="@+id/action_settings"
        android:icon="@drawable/ic_baseline_settings_24"
        android:onClick="onGoToSettings"
        android:title=""
        app:showAsAction="always" />
</menu>